# 控制工程原理课程设计报告

## 1. 课题名称

基于 STM32 与角位移传感器的小车倒立摆控制系统设计

## 2. 摘要

本项目设计并实现了一套基于 STM32F103 单片机、四轮小车平台和角位移传感器的小车倒立摆控制系统。系统以 WHEELTEC D24A 四轮编码器小车为运动平台，以 WDD35D4 角位移传感器测量摆杆倾角，以四路直流电机作为执行机构，通过定时中断周期采样、角度与角速度反馈、速度与位置反馈、PWM 驱动控制和按键调零，完成了单平面倒立摆的基础控制功能。

倒立摆是控制工程中的典型不稳定对象。摆杆在竖直位置附近为不稳定平衡，一旦受到微小扰动便会在重力作用下偏离平衡点，若无反馈控制则会迅速倒下。因此系统必须利用传感器实时检测摆杆姿态，经过控制算法计算后驱动小车沿摆杆倾倒方向运动，通过底座加速度在摆杆底端形成支撑效果，使摆杆被重新拉回竖直附近。

本项目的实现过程并非单纯照搬理论模型，而是围绕真实硬件条件进行了大量工程化设计与调试。例如，针对串口调零不便的问题增加了按键调零；针对小角度时电机“有控制量但推不动”的现象增加了 PWM 死区补偿；针对小车易向单侧慢漂的问题引入了漂移检测与回拉逻辑；针对 SWD 下载口被误关闭、LCD 白屏、启动失联等现象，对引脚复用和初始化顺序进行了修正。整个开发过程体现了控制工程中“模型分析 + 参数整定 + 工程补偿 + 现象验证”的完整链路。

当前系统已经能够完成上电初始化、角度零点标定、2 ms 周期闭环控制、串口与 LCD 调试输出以及超角度保护停机等功能，具备较完整的课程设计成果形态。虽然在小角度持续回中和长期稳定性方面仍有进一步优化空间，但项目已经较好地体现了控制工程原理在嵌入式系统中的综合应用。

关键词：倒立摆；STM32；角位移传感器；反馈控制；PWM；课程设计

## 3. 研究背景与课题意义

### 3.1 倒立摆在控制工程中的地位

倒立摆是控制理论中的经典实验对象，其价值主要体现在以下几个方面：

1. 它具有天然的不稳定性，能直观体现“控制的必要性”。
2. 它既有清晰的理论基础，又有明显的工程复杂性。
3. 它适合验证比例、积分、微分、状态反馈、阻尼、采样周期、延迟和饱和等核心概念。
4. 它能帮助学生理解理论模型与真实硬件之间的差异。

在课堂推导中，倒立摆常被线性化处理，建立在小角度、理想执行机构、无摩擦和高精度传感器等假设之上。但在真实实验中，还会遇到电机死区、驱动饱和、编码器方向不统一、调试接口冲突、零点偏置、机械装配误差、轮胎摩擦不一致等问题。因此，倒立摆课程设计不仅考察控制原理，也考察工程实现能力。

### 3.2 本项目的意义

本项目采用小车平台而非理想滑轨，且使用角位移传感器而非惯性测量单元，具有明显的工程约束。这使得项目更接近“有限条件下如何实现可工作的控制系统”这一真实问题。通过本项目，可以系统训练以下能力：

1. 硬件资源分配与电路连接能力。
2. 传感器数据采集与离散处理能力。
3. 中断驱动程序结构设计能力。
4. 闭环控制器设计与参数整定能力。
5. 对实验现象进行分析、定位和修正的能力。

## 4. 设计任务与指标

结合课程实验条件，本项目的设计任务不是做一个完全工业级的倒立摆产品，而是完成一个可运行、可分析、可讲解的小车倒立摆实验平台。主要任务如下：

1. 使用四轮小车实现单平面倒立控制。
2. 仅控制前后运动，不要求转向。
3. 不包含自动起摆功能，由人工将摆杆放到近似竖直位置后启动。
4. 通过角位移传感器测量摆杆倾角。
5. 通过按键完成零点标定，并开始平衡控制。
6. 通过编码器采集车轮运动信息，构造速度与位置反馈。
7. 通过 LCD 和串口显示系统状态，便于调试与汇报。
8. 当偏角过大时自动停机保护。

当前工程实际运行时，系统满足如下实验性指标：

1. 控制周期为 2 ms。
2. 串口调试刷新周期约为 20 ms。
3. LCD 刷新周期约为 100 ms。
4. 角度过大时可自动停止 PWM 输出。
5. 支持 KEY2 调零启动，KEY3 停机。

## 5. 总体方案设计

### 5.1 系统组成

系统总体由五部分组成：

1. 机械平台。
   包括四轮小车、摆杆、摆杆固定结构和轮系传动结构。
2. 检测单元。
   包括角位移传感器、编码器和电池电压 ADC 采样。
3. 控制单元。
   由 STM32F103 单片机完成采样、计算、控制输出和状态管理。
4. 执行单元。
   包括四路电机驱动与电机 PWM/方向控制。
5. 人机交互单元。
   包括按键、LCD 和串口调试接口。

### 5.2 整体工作思路

系统的工作过程可以概括为：

1. 上电初始化外设。
2. 用户将摆杆扶正。
3. 按下 KEY2 完成当前角度零点标定。
4. TIM1 以 2 ms 周期进入中断。
5. 中断中读取角度传感器和编码器数据。
6. 控制器根据角度、角速度、速度和位置状态计算控制输出。
7. 输出统一 PWM 驱动四个车轮前后运动。
8. 若偏角超限则停机。
9. 主循环负责调试显示和按键处理，不直接参与实时控制。

### 5.3 与直线倒立摆示例程序的关系

本项目参考了 WHEELTEC 直线倒立摆示例程序的基本思想，即：

1. 将控制核心放在定时中断内执行。
2. 把姿态控制放在高优先级实时闭环中。
3. 主循环只承担显示与交互功能。

但由于本项目使用的是四轮小车而非滑轨，因此不能完全照搬直线倒立摆模型。主要差异如下：

1. 执行机构从滑块变为四轮车体，存在更多摩擦与轮差问题。
2. 传感器从惯导类姿态模块转为角位移传感器，角速度需要差分估算。
3. 小车系统的单侧慢漂和死区问题比滑轨平台更明显。
4. 机械结构不具备理想的一维直线约束，需要更多工程补偿。

因此，本项目最终形成的是“参考示例程序结构，但结合小车平台做工程化修正”的方案。

## 6. 硬件系统设计与选型

### 6.1 主控芯片选型

项目选用 STM32F103RCT6 作为主控芯片，原因如下：

1. 主频 72 MHz，性能足够支撑 2 ms 控制周期。
2. 具备丰富的定时器资源，可同时实现 PWM、编码器接口和周期中断。
3. 自带多通道 ADC，适合接入角位移传感器和电压采样。
4. 标准外设库成熟，便于课程设计阶段快速开发。
5. Keil + ST-Link 开发链较稳定，资料丰富。

### 6.2 小车平台选型

项目采用 WHEELTEC D24A 四轮霍尔编码器小车平台。选择理由如下：

1. 自带电机与编码器，便于实现速度反馈。
2. 底盘结构稳定，适合安装摆杆和控制板。
3. 四轮平台空间充足，便于走线和模块集成。
4. 适合课程项目快速搭建。

其不足在于：

1. 四轮车体的运动不如滑轨理想。
2. 左右轮一致性会影响前后运动纯度。
3. 地面摩擦变化对控制影响较大。

### 6.3 角位移传感器选型

项目采用 WDD35D4 角位移传感器，原因如下：

1. 直接输出模拟电压，读取简单。
2. 不需要 I2C/SPI 通信协议和姿态融合算法。
3. 角度信息直观，适合作为课程展示对象。
4. 与现有单片机 ADC 资源匹配良好。

不足之处在于：

1. 角速度不能直接测得，需要差分计算。
2. 对零点精度较敏感。
3. 模拟信号存在噪声与供电敏感性。

### 6.4 电机与驱动设计

系统使用四路 PWM 控制四个电机，并通过方向引脚控制正反转。在平衡控制阶段，四轮采用相同 PWM 输出，即把整个四轮车简化为一个沿前后方向运动的整体。这样做的优点是：

1. 控制器结构更简单。
2. 参数整定更集中。
3. 更适合课程设计演示。

### 6.5 编码器测速方案

本系统使用三路硬件编码器接口加一路软件解码：

1. A 电机：TIM8 硬件编码器。
2. B 电机：TIM2 硬件编码器。
3. C 电机：TIM3 硬件编码器。
4. D 电机：软件正交解码。

其本质上是一种资源折中方案。由于需要同时保留多路 PWM、中断和 ADC 功能，因此没有为四个轮子都配置完全独立的硬件编码器接口。

## 7. 硬件连接与引脚分配

### 7.1 PWM 输出

1. A 电机：PA0，TIM5_CH1
2. B 电机：PA1，TIM5_CH2
3. C 电机：PA2，TIM5_CH3
4. D 电机：PA3，TIM5_CH4

### 7.2 电机方向引脚

1. AIN1：PC13
2. AIN2：PC14
3. BIN1：PB13
4. BIN2：PB12
5. CIN1：PB1
6. CIN2：PB0
7. DIN1：PC1
8. DIN2：PC2

### 7.3 编码器输入

1. E1A：PC6
2. E1B：PC7
3. E2A：PA15
4. E2B：PB3
5. E3A：PA7
6. E3B：PA6
7. E4A：PA8
8. E4B：PA4

### 7.4 角位移传感器

1. 传感器输出：PC4，ADC1_CH14
2. 供电：3.3V
3. 地：GND

### 7.5 其他接口

1. 电池采样：PA5
2. 串口：PA9/PA10
3. LCD：PB4 到 PB9
4. 驱动待机：STBY 接 3.3V

## 8. 被控对象分析与控制原理

### 8.1 倒立摆为什么难控制

摆杆在竖直向上位置附近属于不稳定平衡。若摆杆向某一方向偏离一个小角度，则重力会继续推动其向该方向倒下。若不控制，则角度误差会越来越大。要让其重新回到竖直附近，小车必须沿摆杆倾倒方向快速运动，使摆杆底部“跟上”摆杆重心投影，从而把摆杆重新支撑起来。

因此，倒立摆控制的核心不是让小车“跑得快”，而是让小车在正确时刻产生正确方向的加速度。这个结论和调试中的现象完全一致：如果小车只是持续朝一个方向加速，而没有在适当时机形成有效的反向修正，那么即使速度越来越大，摆杆也不一定会被摆回中心。

### 8.2 负反馈思想

本项目采用多反馈量联合控制。负反馈的基本思想是：

1. 检测当前输出状态。
2. 与目标状态比较，得到误差。
3. 控制器输出一个减少误差的作用量。

在本项目中，目标状态是“摆杆竖直，小车不过度漂移”。因此反馈量包括：

1. 角度误差。
2. 角速度。
3. 小车速度。
4. 小车位置。

### 8.3 角度反馈的作用

角度反馈决定小车该向哪个方向运动，以及应该输出多大基础控制量。角度偏差越大，小车需要越积极地去追杆。

如果角度比例系数过小，会出现以下现象：

1. 小角度扰动没有明显修正。
2. 摆杆不断缓慢偏离中心。
3. 到大角度时才突然猛冲。

如果角度比例系数过大，会出现：

1. 竖直附近高频抖动。
2. 修正过度。
3. 摆杆来回越摆越大。

### 8.4 角速度反馈的作用

角速度反馈本质上是一种微分控制，反映“摆杆正在以多快的速度继续倒下”。

当摆杆角度和角速度同号时，说明摆杆在继续远离平衡点，这是危险状态；当角度和角速度异号时，说明摆杆已经在往回摆。

角速度反馈的作用有两个：

1. 让控制器更早感知“正在倒下”的趋势。
2. 给系统增加阻尼，避免单纯靠角度项导致反应滞后。

但角速度项过大也会带来副作用：

1. 对测量噪声敏感。
2. 容易在竖直附近形成细碎抖动。

### 8.5 速度反馈的作用

如果只有角度和角速度反馈，小车在追杆时可能越跑越快，最终虽然暂时没倒，但会因为持续加速而失去速度余量。速度反馈的作用类似于阻尼项，其作用是：

1. 抑制小车速度无限积累。
2. 在摆杆回正时帮助小车收速。
3. 降低大角度救摆后的反冲。

但是速度反馈过大，会导致“刚想追杆就先被刹住”，从而表现为：

1. 小角度时车不动。
2. 杆子在一侧倒下，小车只能给一点力。
3. 摆杆回不到中心线另一边。

这也是项目后期多次调整 `Balance_Speed_Kp` 和 `Balance_Rescue_Speed_Kp` 的原因。

### 8.6 位置反馈的作用

位置反馈不是直接用来扶杆，而是限制小车整体位置漂移。若没有位置反馈，小车可能在追杆过程中不断偏向某一侧，虽然短时间看似还在控制，但最终会因为距离和速度都被“吃掉”而无法继续救摆。

位置反馈的本质是让小车有“回中”倾向，使其长期平均位置不偏离过大。

### 8.7 为什么本项目没有强调积分项

经典 PID 控制中，积分项用于消除稳态误差。但倒立摆是快速不稳定系统，积分项容易积累历史误差，带来：

1. 明显滞后。
2. 大量能量积累。
3. 小车在错误方向持续加速。

因此当前项目主要依赖角度比例、角速度微分、速度阻尼和位置回中来构建控制器，而没有把积分项作为核心部分。这更符合倒立摆的工程实现特点。

## 9. 软件总体架构设计

### 9.1 分层结构

工程分为如下层次：

1. `CORE`
   ARM Cortex-M3 内核和启动文件。
2. `SYSTEM`
   系统时钟、延时和串口基础功能。
3. `HAREWER`
   包含 ADC、ENCODER、GPIO、LCD、PWM、BALANCE 等模块。
4. `USER`
   主程序、调度逻辑和中断服务。

### 9.2 中断驱动的控制结构

项目后期参考示例程序，将控制核心从主循环移入 TIM1 周期中断。这样做非常关键，原因如下：

1. 主循环中若包含 `printf`、LCD 刷新、按键扫描等低速操作，会拉长控制周期。
2. 倒立摆是快系统，控制周期抖动会显著恶化稳定性。
3. 中断执行更适合做“固定采样、固定运算、固定输出”的离散控制。

当前结构为：

1. TIM1 中断每 2 ms 进入一次，完成实时闭环。
2. 主循环仅处理按键、串口命令和显示刷新。

### 9.3 程序流程

启动后流程如下：

1. 系统时钟初始化。
2. 延时初始化。
3. 串口初始化。
4. LCD 初始化与启动诊断显示。
5. GPIO 初始化。
6. 按键初始化。
7. ADC 初始化。
8. PWM 初始化。
9. 编码器初始化。
10. 编码器辅助定时器初始化。
11. 平衡控制定时器初始化。

之后等待用户调零启动。

## 10. 关键程序设计与代码分析

### 10.1 平衡控制定时器设计

控制周期由 TIM1 产生，核心代码如下：

```c
static void Balance_Timer_Init(void)
{
    TIM_TimeBaseInitTypeDef TIM_TimeBaseStructure;
    NVIC_InitTypeDef NVIC_InitStructure;

    RCC_APB2PeriphClockCmd(RCC_APB2Periph_TIM1, ENABLE);

    TIM_TimeBaseStructure.TIM_Prescaler = 7200 - 1;
    TIM_TimeBaseStructure.TIM_Period = BALANCE_LOOP_MS * 10 - 1;
    TIM_TimeBaseStructure.TIM_ClockDivision = 0;
    TIM_TimeBaseStructure.TIM_CounterMode = TIM_CounterMode_Up;
    TIM_TimeBaseInit(TIM1, &TIM_TimeBaseStructure);

    TIM_ClearFlag(TIM1, TIM_FLAG_Update);
    TIM_ITConfig(TIM1, TIM_IT_Update, ENABLE);

    NVIC_InitStructure.NVIC_IRQChannel = TIM1_UP_IRQn;
    NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = 2;
    NVIC_InitStructure.NVIC_IRQChannelSubPriority = 0;
    NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;
    NVIC_Init(&NVIC_InitStructure);

    TIM_Cmd(TIM1, ENABLE);
}
```

原理说明如下：

1. 系统时钟 72 MHz 先经预分频变为 10 kHz。
2. 周期值由 `BALANCE_LOOP_MS * 10 - 1` 决定。
3. 当 `BALANCE_LOOP_MS = 2` 时，每 2 ms 产生一次更新中断。

这体现了离散控制中“固定采样周期”的基本要求。

### 10.2 按键调零与启动逻辑

项目中新增的按键调零是一个很重要的工程改进，关键代码如下：

```c
static void Calibrate_Angle_Zero(void)
{
    Balance_Timer_Set(0);
    Balance_Stop();
    Set_PWM(0, 0, 0, 0);
    zero_offset = Get_adc_Average(ADC_Channel_14, 50);
    angle_adc = zero_offset;
    angle_x100 = 0;
    Balance_Timer_Set(1);
}
```

这段代码体现了几个设计原则：

1. 调零时先停止控制中断，避免 ADC 被控制任务占用。
2. 调零时先关闭 PWM，避免采样过程中小车误动作。
3. 对角位移传感器采样 50 次求平均，提高零点稳定性。
4. 调零后再恢复控制定时器，使零点更新立刻生效。

按键处理部分如下：

```c
static void Handle_Key(u8 key)
{
    if(key == KEY_ZERO_PRESS)
    {
        Calibrate_Angle_Zero();
        Balance_Start();
        printf("Zero set by KEY2, balance start! ADC=%d\r\n", zero_offset);
    }
    else if(key == KEY_AUX_PRESS)
    {
        Balance_Stop_Reason = BALANCE_STOP_USER;
        Balance_Stop();
        Set_PWM(0, 0, 0, 0);
        printf("Balance stopped by KEY3.\r\n");
    }
}
```

其中：

1. KEY2 对应调零并启动。
2. KEY3 对应手动停机。

这使系统更适合现场实验和答辩演示。

### 10.3 角度计算方法

系统从角位移传感器读取 ADC 值，再换算为角度：

```c
angle_adc = Get_adc(ADC_Channel_14);
angle_x100 = ((int)angle_adc - zero_offset) * 34000 / 4096;
```

原理解释：

1. ADC 量程为 0 到 4095。
2. 传感器有效角度量程近似按 0 到 340 度处理。
3. `angle_x100` 表示百分度，即 1 度记为 100。

这样做的优点是：

1. 保留两位小数精度。
2. 避免大量浮点运算。
3. 便于阈值比较与整型控制。

### 10.4 角速度估算算法

本项目没有直接测量角速度，而是通过差分近似微分：

```c
Balance_Angle_Rate_X100 = (angle_x100 - last_angle_x100) * 5 / BALANCE_LOOP_MS;
```

原理如下：

1. `angle_x100 - last_angle_x100` 表示相邻两次采样之间的角度变化。
2. 再除以采样周期，就可近似得到角速度。
3. 乘以比例因子是为了保持原有标度一致。

这正是离散控制中“用差分逼近微分”的典型方法。

### 10.5 速度与位置估算算法

速度计算采用四轮编码器平均值：

```c
raw_speed = (encoder[0] + encoder[1] + encoder[2] + encoder[3]) / 4;
speed = raw_speed * 5 / BALANCE_LOOP_MS;
Balance_Position += speed;
Balance_Position = Balance_Limit(Balance_Position, 200000);
Balance_Speed_Filter = (Balance_Speed_Filter + speed) / 2;
```

其含义如下：

1. 四轮平均作为车体整体速度估计值。
2. 位置量由速度累积得到，本质上是离散积分。
3. `Balance_Speed_Filter = (旧值 + 新值)/2` 是一阶平滑滤波。

这里的速度滤波虽然简单，但能降低编码器瞬时波动对控制器的影响。

### 10.6 平衡控制器核心结构

当前控制器的核心代码为：

```c
angle_pwm = angle_kp * angle_x100 + angle_kd * Balance_Angle_Rate_X100;

if(falling_away)
{
    angle_pwm += Balance_Fall_Rate_Kd * Balance_Angle_Rate_X100;
}

if(abs_angle > BALANCE_RESCUE_ANGLE_X100)
{
    rescue_pwm = Balance_Rescue_Kp * (abs_angle - BALANCE_RESCUE_ANGLE_X100) * Balance_Sign(angle_x100);
}

speed_pwm = speed_kp * Balance_Speed_Filter;
position_pwm = (drifting ? Balance_Drift_Position_Kp : Balance_Position_Kp) * Balance_Position;

Balance_Output = BALANCE_OUTPUT_SIGN * (int)(angle_pwm + rescue_pwm - speed_pwm - position_pwm);
```

这可以理解为：

`输出 = 角度项 + 角速度项 + 倒下趋势增强项 + 大角度救摆项 - 速度阻尼项 - 位置回中项`

其控制思想非常符合实际调试需求：

1. 平时以角度和角速度为主。
2. 角度大时增强追杆能力。
3. 小车速度和位置过大时增加阻尼与回拉。

### 10.7 “继续倒下”状态判断算法

系统定义了 `falling_away` 状态：

```c
falling_away = (angle_x100 != 0 && Balance_Angle_Rate_X100 != 0 &&
               Balance_Sign(angle_x100) == Balance_Sign(Balance_Angle_Rate_X100));
```

原理解释：

1. 若角度为正且角速度也为正，说明摆杆正在向正方向继续倒下。
2. 若角度为负且角速度也为负，说明摆杆正在向负方向继续倒下。
3. 这两种情况都属于“远离平衡点”的危险状态。

利用该状态，程序会额外加大角速度补偿，形成更强的追杆加速度。

### 10.8 小角度保持区算法

为减小竖直附近抖动，系统加入了小角度保持区：

```c
if(!drifting && abs_angle < BALANCE_HOLD_ANGLE_X100 &&
   Balance_Abs(Balance_Angle_Rate_X100) < BALANCE_HOLD_RATE_X100)
{
    Balance_Output = 0;
    return 0;
}
```

这段逻辑的含义是：

1. 若当前没有漂移；
2. 且角度很小；
3. 且角速度也很小；
4. 则认为当前处于近似静止平衡，直接输出 0。

该逻辑的目的是避免在极小扰动下不断来回抽动，但如果这个区域过大，也会带来“小角度不动、等大了再猛冲”的问题，因此它必须与最小输出补偿和漂移判断配合使用。

### 10.9 最小输出补偿算法

为了克服电机与地面静摩擦，程序设计了最小输出补偿：

```c
if(!drifting && falling_away && abs_angle > BALANCE_START_ANGLE_X100 &&
   Balance_Output != 0 && Balance_Abs(Balance_Output) < BALANCE_MIN_OUTPUT)
{
    min_output = BALANCE_MIN_OUTPUT;
    if(abs_angle < BALANCE_SOFT_ANGLE_X100)
    {
        min_output = BALANCE_MIN_OUTPUT *
            (abs_angle - BALANCE_START_ANGLE_X100) /
            (BALANCE_SOFT_ANGLE_X100 - BALANCE_START_ANGLE_X100);
    }
    if(Balance_Abs(Balance_Output) < min_output)
    {
        Balance_Output = min_output * Balance_Sign(Balance_Output);
    }
}
```

其含义是：

1. 当控制器判断应该追杆，但输出太小时，电机可能推不动车。
2. 此时强制把输出提升到一个最低可动作水平。
3. 为避免突跳，最小输出不是立刻给满，而是随角度线性爬升。

这是一个非常典型的工程补偿，专门用于解决“理论上应该动，实际上静摩擦不让动”的问题。

### 10.10 漂移检测与回拉算法

小车慢慢向一侧漂移是本项目中最难缠的问题之一，因此增加了漂移检测：

```c
drifting = (Balance_Abs(Balance_Speed_Filter) > BALANCE_DRIFT_SPEED ||
           Balance_Abs(Balance_Position) > BALANCE_DRIFT_POSITION);
```

当判定已发生漂移后，程序会增加位置回中增益，并在必要时强制反向回拉：

```c
if(!kicking_now && drifting && Balance_Position != 0 &&
   abs_angle < BALANCE_DRIFT_RECENTER_ANGLE_X100 &&
   Balance_Sign(Balance_Output) == Balance_Sign(Balance_Position))
{
    Balance_Output = -BALANCE_OUTPUT_SIGN * BALANCE_DRIFT_PULL_PWM * Balance_Sign(Balance_Position);
}
```

原理解释如下：

1. 当车已经在某一方向偏离中心时，若当前输出还在继续把车往更远处推，则说明控制器陷入了“同向累积”。
2. 此时强制给一个反向拉回输出，使系统重新获得回中趋势。

这段逻辑正是针对“小角度时小车一直向一个方向慢慢加速”的问题而设计的。

### 10.11 急加速脉冲救摆算法

在调试中发现，大角度时若只慢慢加速，摆杆可能根本回不到中心线另一侧。因此系统引入了“急加速脉冲”：

```c
kick_ready = drifting && falling_away &&
    abs_angle > BALANCE_KICK_ANGLE_X100 &&
    Balance_Abs(Balance_Speed_Filter) > BALANCE_KICK_SPEED &&
    kick_ticks == 0 && recover_ticks == 0 && kick_cooldown_ticks == 0;

if(kick_ready)
{
    kick_direction = Balance_Sign(angle_x100);
    kick_ticks = BALANCE_KICK_TICKS;
}
```

触发后：

```c
if(kicking_now)
{
    Balance_Output = BALANCE_KICK_PWM * kick_direction;
    kick_ticks--;
    if(kick_ticks == 0)
    {
        recover_ticks = BALANCE_RECOVER_TICKS;
        kick_cooldown_ticks = BALANCE_KICK_COOLDOWN_TICKS;
    }
}
```

其控制思想是：

1. 当系统已经偏移且继续倒下时，单纯柔和控制可能不够。
2. 这时短时间输出一个较大加速度，快速把摆杆打回另一侧。
3. 随后进入恢复阶段，避免刚打回去又立刻反冲。

从控制角度看，这是一个“状态切换 + 非线性补偿”的策略。

### 10.12 串口调试输出设计

为了帮助分析问题，系统通过串口持续输出内部状态：

```c
printf("BAL=%d Stop=%d V=%.2f A=%d B=%d C=%d D=%d PWM=%d Ang=%d.%02d Rate=%d SF=%d Pos=%d Bias=%d ADC=%d Off=%d\r\n",
    Balance_Enable, Balance_Stop_Reason, vcc,
    encoder[0], encoder[1], encoder[2], encoder[3],
    balance_pwm,
    angle_x100 / 100, (angle_x100 < 0 ? -angle_x100 : angle_x100) % 100,
    Balance_Angle_Rate_X100, Balance_Speed_Filter, Balance_Position, Balance_Angle_Bias_X100, angle_adc, zero_offset);
```

这些数据的意义如下：

1. `Ang`：当前摆杆角度。
2. `Rate`：角速度估计。
3. `SF`：速度滤波值。
4. `Pos`：位置积分值。
5. `Bias`：角度偏置估计值。
6. `PWM`：控制输出。

这些量在调试过程中发挥了非常关键的作用，尤其是定位“小车为什么一直往一边跑”时，`SF` 和 `Pos` 的符号关系非常重要。

## 11. 控制算法详细讲解

### 11.1 角度 PD 控制

本项目的主控制器本质上是以角度为核心的 PD 控制。

一般形式为：

`u(k) = Kp * e(k) + Kd * [e(k) - e(k-1)]`

其中：

1. `e(k)` 是第 `k` 次采样时的角度误差。
2. `Kp` 决定对当前偏差的响应强度。
3. `Kd` 决定对变化趋势的响应强度。

在本项目中：

1. `angle_x100` 充当角度误差。
2. `Balance_Angle_Rate_X100` 充当离散微分项。
3. 二者合成基础平衡控制量。

### 11.2 救摆增强算法

大角度时，单纯 PD 往往不够，因为摆杆已经接近失稳。项目因此引入阈值式救摆增强：

1. 当角度超过救摆阈值后；
2. 额外增加与超出角度成比例的补偿；
3. 使控制器更偏向“先别倒”而不是“动作要优雅”。

这是一种典型的分段控制思想。

### 11.3 速度阻尼算法

速度项的控制作用类似于机械阻尼器。其本质是：

1. 当车速大时，抵消一部分输出。
2. 当车速小或正在紧急救摆时，适当减小阻尼。

因此本项目不是单一固定的 `speed_kp`，而是根据状态切换：

1. 正常时使用 `Balance_Speed_Kp`。
2. 大角度时使用 `Balance_Rescue_Speed_Kp`。
3. 继续倒下时可进一步降低速度阻尼。
4. 漂移明显时再提高回拉阻尼。

这说明实际控制器往往不是单一公式，而是“主控制 + 状态调度”的组合结构。

### 11.4 位置回中算法

位置项来源于对速度的离散积分，其作用是让车不要无限远离中点。位置回中算法体现的是低频约束：

1. 角度控制负责快速扶杆。
2. 位置控制负责长期回中。

如果位置项过大，会导致：

1. 摆杆还没扶稳，小车就急着往回收。
2. 救杆动作被抵消。

如果位置项过小，会导致：

1. 小车长期偏向一边。
2. 行程和速度余量逐步耗尽。

### 11.5 非线性补偿算法

项目中使用的非线性补偿主要包括：

1. 小角度保持区。
2. 最小输出补偿。
3. 大角度救摆增强。
4. 漂移回拉。
5. 急加速脉冲。
6. 超角度保护停机。

这些算法的共同特点是：

1. 它们不属于标准线性 PID。
2. 它们专门用来处理真实硬件中的特殊现象。
3. 它们能明显改善实验效果。

因此，本项目可以看作“经典反馈控制 + 非线性工程补偿”的综合实现。

## 12. 开发过程中的典型问题与解决方法

这一部分是本报告的重点之一，因为课程设计不仅要给出结果，更要说明问题是如何发现、如何分析、如何解决的。

### 12.1 问题一：通过串口调零不方便

现象：

1. 实验时需要一只手扶杆，一只手操作。
2. 串口命令方式不够直接，现场操作容易失败。

分析：

1. 调零必须在摆杆近似竖直时立即执行。
2. 串口交互增加了额外操作链路。

解决方法：

1. 增加 KEY2 调零功能。
2. 按下 KEY2 后自动读取当前角度并设为零点。
3. 调零后立即启动平衡控制。

效果：

1. 提高了实验效率。
2. 更适合课程演示。

### 12.2 问题二：方向逻辑相反

现象：

1. 杆子向前倾时，小车反而后退。
2. 一上电控制就明显错误。

分析：

1. 控制输出符号定义与电机方向定义不一致。
2. 传感器角度正负方向与车体实际前后方向不匹配。

解决方法：

1. 检查电机方向与控制符号的映射。
2. 修正整体输出方向定义。

控制工程启示：

反馈系统中符号错误最致命。负反馈一旦变成正反馈，系统必然快速发散。

### 12.3 问题三：小角度时有输出但车不动

现象：

1. 串口中已经有 PWM 输出。
2. 但小车几乎没有动作。
3. 摆杆只会继续偏大，直到大角度才突然猛冲。

分析：

1. 控制器理论输出已经存在。
2. 但电机静摩擦、减速箱阻力和轮胎地面摩擦构成了死区。
3. 小 PWM 无法真正驱动车轮。

解决方法：

1. 在 `Set_PWM()` 中增加死区补偿。
2. 设置最小输出阈值和线性爬升段。
3. 让小角度控制量既能推动小车，又不至于突跳过猛。

控制工程启示：

执行机构非线性是实际控制系统中的常见问题。控制器设计不能只看数学输出，还要看执行机构是否真的“动起来”。

### 12.4 问题四：小车冲得过快，过冲严重

现象：

1. 一旦修正，小车猛冲。
2. 摆杆被甩到另一边。
3. 系统形成来回大幅摆动。

分析：

1. 角度比例和微分过强。
2. 最大 PWM 限幅过大。
3. 速度阻尼不足。

解决方法：

1. 适当降低角度项和微分项参数。
2. 降低 `BALANCE_PWM_LIMIT`。
3. 提高速度反馈权重。

控制工程启示：

快速性和稳定性是一对矛盾。追求响应太快，往往以过冲和振荡为代价。

### 12.5 问题五：响应太慢，追不上杆

现象：

1. 杆子开始偏离时小车没有明显动作。
2. 等小车开始加速时已经来不及。

分析：

1. 小角度起控阈值过大。
2. 最小输出补偿不足。
3. 采样与控制周期不够快。

解决方法：

1. 降低 `BALANCE_START_ANGLE_X100`。
2. 提高最小输出补偿。
3. 将控制周期从 5 ms 缩短到 2 ms。
4. 串口刷新改为约 20 ms，LCD 刷新改为约 100 ms，减少调试链路干扰。

控制工程启示：

延迟是稳定性的敌人。对于快速不稳定系统，控制周期的缩短常常比单纯加大参数更有效。

### 12.6 问题六：大角度能救，小角度却一直往一侧慢跑

现象：

1. 大角度时能看到明显左右修正。
2. 小角度时却形成“加速一下、停一下、再加速”的循环。
3. 最终小车整体持续向某一侧漂移。

分析：

这是本项目最核心的问题之一。通过多轮观察和代码分析，发现原因并非单纯某个 PID 参数不合适，而是多个机制叠加导致：

1. 小角度保持区会在看似平稳时直接输出 0。
2. 若位置量被清空，则回中信息消失。
3. 若运行时自动偏置学习开启，真实小角度误差可能被逐渐学成“零点”。
4. 若输出仍与漂移方向同向，则系统会持续积累速度和位置。

解决方法：

1. 关闭自动偏置学习：
   `BALANCE_AUTO_BIAS_ENABLE = 0`
2. 修改 hold 区逻辑，只在未漂移时才允许输出归零。
3. 不再在 hold 区清空位置量。
4. 增加漂移检测。
5. 增加反向回拉逻辑。
6. 增加位置和速度状态的串口输出，观察 `SF`、`Pos` 与 `PWM` 的符号关系。

控制工程启示：

复杂问题往往不是“一个参数调错了”，而是多个逻辑在某种状态下共同产生了错误趋势。

### 12.7 问题七：摆杆已经明显倒向一边，但小车只是慢慢加速

现象：

1. 小车一直朝摆杆倾倒方向加速。
2. 但摆杆只被拉回一点点，并未跨过中心线。
3. 最终速度越来越大，仍然救不回来。

分析：

这是因为恒定或缓慢变化的加速度不足以迅速改变摆杆的运动趋势。摆杆要真正回到另一侧，常常需要一个更强的短时加速度过程。

解决方法：

1. 增加“继续倒下”状态判断。
2. 在该状态下增强角速度项。
3. 引入短时急加速脉冲 `kick`。
4. 在脉冲后增加恢复阶段，避免立刻反冲。

控制工程启示：

对于非线性快系统，单一线性连续控制并不总够用，状态切换和事件触发式控制往往更有效。

### 12.8 问题八：LCD 白屏

现象：

1. 程序烧录后 LCD 背光亮，但屏幕全白。

分析：

1. LCD 的 SCLK 使用了 `PB4`。
2. `PB4` 默认与 JTAG 功能相关。
3. 若在 LCD 初始化前未正确释放 `PB4`，LCD 时钟线就不可用。

解决方法：

1. 在 LCD GPIO 初始化时提前配置 AFIO。
2. 调用 `GPIO_Remap_SWJ_JTAGDisable`，只关闭 JTAG，保留 SWD。
3. 且必须在配置 LCD GPIO 前执行。

控制工程启示：

嵌入式系统中很多“控制问题”其实根源在硬件复用和初始化顺序。

### 12.9 问题九：程序运行后板子仿佛“卡死在 BOOT0”

现象：

1. 烧录后程序似乎无法正常启动。
2. ST-Link 常常失联。
3. 只有靠 BOOT0 或复位时抢连接才能重新下载。

分析：

问题根源不是 BOOT0，而是程序运行后关闭了 SWD 调试口。原来编码器初始化中使用了：

```c
GPIO_Remap_SWJ_Disable
```

该配置会同时关闭 JTAG 和 SWD。一旦程序运行到这里，下载调试口被关掉，表现就像“板子坏了”。

解决方法：

1. 改为：

```c
GPIO_Remap_SWJ_JTAGDisable
```

2. 只关闭 JTAG，保留 SWD。

控制工程启示：

调试链路本身也是系统工程的一部分。一个引脚复用配置错误，足以让后续所有调试工作瘫痪。

### 12.10 问题十：Keil 报 `USART_RX_STA` 类型冲突

现象：

编译时报错，`usart.c` 中 `USART_RX_STA` 的定义与头文件声明不一致。

分析：

头文件声明为 `volatile u16 USART_RX_STA`，源文件定义却缺少 `volatile`。

解决方法：

保持定义与声明一致，统一为 `volatile u16`。

控制工程启示：

实时系统中的状态量常被中断和主循环共同访问，`volatile` 是必要的编译语义保证。

## 13. 为什么最后采用现在这套控制逻辑

在调试过程中，项目其实经历过很多版本。之所以最后形成当前控制逻辑，是因为它在“能救摆”和“不过度抽动”之间取得了相对平衡。其核心思想可以概括为：

1. 用角度和角速度作为主反馈，保证对姿态变化足够敏感。
2. 用速度反馈给系统阻尼，防止越跑越快。
3. 用位置反馈限制长期偏移。
4. 用最小输出补偿解决小命令推不动车的问题。
5. 用漂移回拉解决小角度单向慢跑问题。
6. 用急加速脉冲解决大角度时“加速了也救不回来”的问题。
7. 用安全保护避免完全失控时继续冲击。

这说明当前算法不是单一 PD，也不是标准 PID，而是一个面向实际问题逐步叠加形成的复合控制策略。

## 14. 当前程序运行逻辑总结

当前程序刷入后，实际运行逻辑如下：

1. 上电初始化系统时钟、串口、LCD、按键、ADC、PWM 和编码器。
2. 启动阶段 LCD 显示诊断信息。
3. 初始化完成后进入待机状态。
4. 用户将摆杆扶到近似竖直位置。
5. 用户按下 KEY2。
6. 程序停止中断控制，采样角位移传感器平均值作为零点。
7. 重置控制状态并进入平衡控制模式。
8. TIM1 每 2 ms 完成一次：
   采角度、读编码器、算角速度、算速度位置、执行控制器、输出 PWM。
9. 主循环中每隔一定周期刷新 LCD、输出串口变量并处理按键。
10. 若用户按下 KEY3，则系统停止平衡输出。
11. 若摆杆偏离超出安全角度，则自动停机保护。

## 15. 当前成果与不足

### 15.1 当前成果

目前项目已经完成：

1. STM32 底层外设驱动与多模块集成。
2. 四轮编码器读取与速度估算。
3. 角位移传感器采样与角度换算。
4. 按键调零与按键停机。
5. 2 ms 定时中断闭环控制。
6. 串口调试与 LCD 状态显示。
7. 多种工程化控制补偿策略。
8. 安全保护与调试链路修复。

### 15.2 目前不足

虽然系统已经能较好体现倒立摆控制原理，但仍存在以下不足：

1. 小角度下仍有一定概率慢慢偏向一边。
2. 对摆杆质量、长度和机械装配敏感。
3. 小车平台不如滑轨理想，轮差和摩擦影响明显。
4. 角速度由差分估算，噪声敏感性仍然存在。
5. 当前控制器主要是工程调参与补偿的结果，尚未建立完整数学状态模型。

## 16. 后续改进方向

### 16.1 机械方面

1. 继续优化摆杆质量和长度。
2. 提高小车左右轮一致性。
3. 优化轮胎与地面的摩擦条件。

### 16.2 传感方面

1. 增加角度滤波。
2. 提高零点标定精度。
3. 若条件允许，可增加陀螺仪辅助测量角速度。

### 16.3 控制算法方面

1. 建立更严格的小车倒立摆线性化模型。
2. 尝试串级控制或状态反馈设计。
3. 对速度和位置项进行更系统的参数分配。
4. 设计更规范的抗饱和策略。

### 16.4 软件工程方面

1. 将参数做成集中配置表。
2. 增加实验记录与参数版本管理。
3. 将显示层与控制层进一步解耦。

## 17. 结论

本课程设计完成了一套基于 STM32F103、WDD35D4 角位移传感器和四轮编码器小车平台的小车倒立摆系统。项目实现了从硬件选型、引脚连接、角度采样、按键调零、编码器测速、定时中断闭环控制，到串口/LCD 调试和超角度保护的完整功能链路。

更重要的是，本项目不仅仅“做出了一个程序”，而是较完整地经历了控制工程实践的全过程：先理解被控对象的不稳定本质，再建立基本控制框架，然后围绕真实实验现象不断分析、调参和修正，最终形成一套可运行的工程化控制方案。

项目调试过程中出现的小角度漂移、速度积累、推不动车、LCD 白屏、SWD 失联和 BOOT0 误判等问题，都成为了课程设计的重要组成部分。它们说明控制系统设计从来不是只调几个 PID 参数，而是包含了对象分析、传感测量、执行机构特性、软件架构、硬件约束和调试方法等多方面内容。

总体来看，本项目达到了控制工程原理课程设计的主要目标，具有较强的实验性、综合性和可汇报性。

## 18. 答辩与 PPT 建议

后续做 PPT 时，建议按如下结构组织：

1. 研究背景与课题意义
2. 系统总体方案
3. 硬件选型与引脚设计
4. 倒立摆控制原理
5. 软件架构与 2 ms 中断闭环
6. 关键算法讲解
7. 典型代码片段说明
8. 开发中的主要问题与解决方法
9. 当前实验效果与不足
10. 总结与改进方向

如果需要在答辩中突出工程性，建议专门做一页“现象 - 原因 - 对策”总结页，效果会很好。
